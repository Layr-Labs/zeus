import {describe, test} from '@jest/globals';

describe('github metadata store', () => {
    test('login/isLoggedIn works.', () => {
        // TODO:(tests)
    });
    test('push works.', () => {
        // TODO:(tests)
    });
    test('getFile works.', () => {
        // TODO:(tests)
    });
    test('updateFile works.', () => {
        // TODO:(tests)
    });
});