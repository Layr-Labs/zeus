import {describe, expect, test} from '@jest/globals';

describe('run', () => {
  test('skip', () => {
    // TODO:(test)
  });
  test('advance', () => {
    // TODO:(test)
  });
  test('various scenarios', () => {
    // TODO:(test)
  });
});