

- will invoke with `--ledger` for ledger signing
- will pass in the pk for eoa signing
- for multisig, passing in dummy pk for signing and giving senderAddress
    - (call vs. delegateCall in gnosis api?)

create(senderAddress, pathToJsonConfig) {

}

queue(senderAddress, pathToJsonConfig) {

}

execute(senderAddress, pathToJsonConfig) {

}


